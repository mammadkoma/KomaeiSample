<MudDialog Class="text-center">
    <DialogContent>
        <MudAlert Severity="Severity.Normal" ContentAlignment="HorizontalAlignment.Center" Icon="@Icons.Material.Filled.Mail" Class="mt-1">
            @CategoriesEnum.Office.GetDescription()
        </MudAlert>

        <MudDataGrid Items="EnvelopeOfficeDtos" Bordered Hover Dense Striped SortMode="SortMode.None" FixedHeader Height="@WindowHeight" Class="mt-1">
            <Columns>
                <PropertyColumn Property="x => x.ModelTitle" Title="مدل" />
                <PropertyColumn Property="x => x.PaperTitle" Title="کاغذ" />
                <PropertyColumn Property="x => x.GrammageTitle" Title="گرماژ" />
                <PropertyColumn Property="x => x.CountTitle" Title="تعداد" />
                <PropertyColumn Property="x => x.HasInternalTeram.ToHasOrNotTitle()" Title="ترام داخلی 50% طوسی" />
                <PropertyColumn Property="x => x.HasDoorGlue.ToHasOrNotTitle()" Title="چسب درب" />
                <PropertyColumn Property="x => x.Price" Title="قیمت (ریال)" Format="#,0" />
            </Columns>
        </MudDataGrid>
    </DialogContent>
    <DialogActions>
        <MudButton Variant="Variant.Filled" OnClick="DownloadPdf" StartIcon="@Icons.Material.Filled.CloudDownload" Class="mb-3 me-5">
            دانلود فایل pdf
        </MudButton>
    </DialogActions>
</MudDialog>